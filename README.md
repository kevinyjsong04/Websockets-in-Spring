# Websockets-in-Spring

#### To run, navigate to the root folder and first run the run.bat file
#### Then, navigate to port 8080 on localhost. You can run ```Start-Process "http://localhost:8080"``` in a separate terminal if that doesn't work.
